# 武陵四秀3日遊 - 系統總整理

## 📋 專案概述

這是一個專為登山活動設計的協作平台，提供行程管理、裝備清單、成員資訊等功能，採用手繪風格 UI，支援多人即時協作。

**技術棧：**
- **前端框架：** Next.js 16 (App Router)
- **語言：** TypeScript
- **樣式：** Tailwind CSS
- **後端服務：** Supabase (PostgreSQL + Realtime + Storage + Auth)
- **部署平台：** Vercel
- **字體：** Google Fonts (Kiwi Maru, Zen Maru Gothic, Hachi Maru Pop)

**專案網址：** https://tourist-lake-one.vercel.app

---

## 🎯 核心功能

### 1. 用戶認證系統
- ✅ 用戶註冊（需 Email 確認）
- ✅ 用戶登入/登出
- ✅ Email 確認流程（含成功通知）
- ✅ 用戶資料管理（姓名、顯示名稱）
- ✅ 帳號刪除功能（管理員專用）

**相關文件：**
- `EMAIL_CONFIRMATION_SETUP.md` - Email 確認設定
- `EMAIL_TEMPLATE_MODIFICATION_STEPS.md` - Email 模板修改
- `DELETE_ACCOUNT_FEATURE.md` - 帳號刪除功能

### 2. 活動介紹頁面 (`/`)
- ✅ **倒數計時便條紙** - 顯示距離出發日期的倒數天數
- ✅ **海拔剖面圖** - 手繪風格的路線圖（可上傳自訂圖片）
- ✅ **活動簡介** - 活動描述與交通資訊（管理員可編輯）
- ✅ **團隊邀請票** - 生成邀請連結，支援複製與分享

**相關組件：**
- `components/ActivityIntro.tsx`
- `components/CountdownSticky.tsx`
- `components/ElevationProfile.tsx`
- `components/TeamInviteTicket.tsx`

### 3. 行程管理 (`/itinerary`)
- ✅ **共享行程時間軸** - 所有成員看到相同的行程
- ✅ **管理員編輯權限** - 僅管理員可新增/編輯/刪除行程點
- ✅ **手繪風格 UI** - 符合整體設計風格

**相關組件：**
- `components/ItineraryTimeline.tsx`
- `hooks/useSharedItinerary.ts`

**相關文件：**
- `SHARED_ITINERARY_SETUP.md`

### 4. 裝備管理 (`/gear`)
- ✅ **個人裝備清單** - 分類管理（必備、建議、選配）
- ✅ **共享裝備認領** - 團隊共享裝備，成員可認領
- ✅ **預設裝備模板** - 管理員可設定新成員的預設裝備清單
- ✅ **即時同步** - 使用 Supabase Realtime 即時更新

**相關組件：**
- `components/GearList.tsx`
- `components/SharedGearClaim.tsx`
- `components/AdminGearTemplateManager.tsx`
- `hooks/useGearItems.ts`
- `hooks/useSharedGearState.ts`
- `hooks/useDefaultGearTemplates.ts`

### 5. 成員資訊 (`/members`)
- ✅ **安全御守** - 3D 翻轉卡片，顯示緊急聯絡資訊
  - 留守人（姓名、電話）
  - 保險（保單號碼）
  - 轄區警局（自動填入）
- ✅ **團隊角色** - 四種角色選擇（領隊、廚師、攝影師、旅行者）
- ✅ **參與者列表** - 顯示已選擇角色的成員
- ✅ **資訊設定頁面** (`/members/setup`) - 填寫並鎖定個人資訊

**相關組件：**
- `components/SafetyOmamori.tsx`
- `components/TeamRoles.tsx`
- `components/EmergencyInfoSetup.tsx`
- `components/RoleSelection.tsx`
- `components/ParticipantList.tsx`

### 6. 管理員功能 (`/admin`)
- ✅ **預設裝備模板管理** - 設定新成員的預設裝備
- ✅ **行程設定管理** - 管理出發日期、邀請碼、海拔圖、活動簡介
- ✅ **成員列表** - 查看所有成員資訊
- ✅ **權限解鎖控制** - 允許成員編輯已鎖定的資訊
- ✅ **用戶權限管理** - 授予/撤銷其他用戶的管理員權限
- ✅ **帳號刪除** - 刪除其他用戶的帳號

**相關組件：**
- `components/TripSettingsManager.tsx`
- `components/AdminGearTemplateManager.tsx`
- `components/MemberList.tsx`
- `components/AdminUnlockControl.tsx`
- `components/UserPermissionManager.tsx`

**相關文件：**
- `ADMIN_SETUP.md` - 管理員設定
- `USER_PERMISSION_MANAGEMENT.md` - 權限管理
- `HOW_TO_GRANT_ADMIN_PERMISSION.md` - 如何授予管理員權限

### 7. 團隊邀請功能 (`/invite/[code]`)
- ✅ **邀請碼系統** - 管理員可設定團隊邀請碼
- ✅ **邀請連結生成** - 自動生成邀請連結
- ✅ **分享功能** - 支援複製連結與 Web Share API
- ✅ **邀請頁面** - 處理邀請碼驗證與用戶加入

**相關組件：**
- `components/TeamInviteTicket.tsx`
- `app/invite/[code]/page.tsx`

**相關文件：**
- `INVITE_FEATURE_GUIDE.md` - 邀請功能使用指南
- `INVITE_CODE_DIFFERENCES.md` - 邀請碼說明

---

## 🗂️ 專案結構

```
tourist/
├── app/                          # Next.js App Router 頁面
│   ├── page.tsx                  # 首頁（活動介紹）
│   ├── layout.tsx                 # 根布局
│   ├── globals.css                # 全域樣式
│   ├── itinerary/                 # 行程頁面
│   ├── gear/                      # 裝備頁面
│   ├── members/                   # 成員頁面
│   │   └── setup/                 # 成員資訊設定
│   ├── admin/                     # 管理員頁面
│   ├── invite/[code]/             # 邀請頁面
│   └── auth/callback/             # Email 確認回調
│
├── components/                    # React 組件
│   ├── ActivityIntro.tsx          # 活動介紹
│   ├── CountdownSticky.tsx        # 倒數計時
│   ├── ElevationProfile.tsx        # 海拔剖面圖
│   ├── TeamInviteTicket.tsx       # 邀請票
│   ├── ItineraryTimeline.tsx      # 行程時間軸
│   ├── GearList.tsx               # 個人裝備清單
│   ├── SharedGearClaim.tsx        # 共享裝備認領
│   ├── SafetyOmamori.tsx          # 安全御守
│   ├── TeamRoles.tsx               # 團隊角色
│   ├── EmergencyInfoSetup.tsx     # 緊急資訊設定
│   ├── RoleSelection.tsx           # 角色選擇
│   ├── ParticipantList.tsx         # 參與者列表
│   ├── AdminGearTemplateManager.tsx # 裝備模板管理
│   ├── TripSettingsManager.tsx     # 行程設定管理
│   ├── MemberList.tsx              # 成員列表
│   ├── AdminUnlockControl.tsx      # 權限解鎖控制
│   ├── UserPermissionManager.tsx   # 用戶權限管理
│   ├── UserProfile.tsx             # 用戶資料
│   ├── Auth.tsx                    # 認證表單
│   └── Navigation.tsx              # 底部導航
│
├── hooks/                         # 自訂 React Hooks
│   ├── useAuth.ts                 # 認證狀態
│   ├── useAdmin.ts                # 管理員權限檢查
│   ├── useGearItems.ts             # 個人裝備
│   ├── useSharedGearState.ts       # 共享裝備
│   ├── useDefaultGearTemplates.ts  # 預設裝備模板
│   ├── useSharedItinerary.ts       # 共享行程
│   ├── useTripSettings.ts          # 行程設定
│   └── useMembers.ts               # 成員列表
│
├── lib/supabase/                   # Supabase 客戶端
│   ├── client.ts                  # 客戶端 Supabase
│   └── server.ts                   # 伺服器端 Supabase
│
├── supabase/                       # 資料庫腳本
│   ├── schema.sql                 # 主要資料表結構
│   ├── migration_*.sql             # 資料庫遷移腳本
│   └── *.sql                       # 各種功能腳本
│
├── public/images/                  # 靜態圖片資源
│   ├── omamori/                    # 御守圖片
│   └── roles/                      # 角色圖片
│
└── *.md                            # 文件說明
```

---

## 🗄️ 資料庫結構

### 主要資料表

1. **`user_profiles`** - 用戶資料
   - `user_id` (UUID, PK)
   - `display_name` (TEXT)

2. **`user_gear_items`** - 個人裝備清單
   - `id` (UUID, PK)
   - `user_id` (UUID, FK)
   - `name` (TEXT)
   - `category` (TEXT)
   - `checked` (BOOLEAN)

3. **`shared_gear`** - 共享裝備
   - `id` (UUID, PK)
   - `name` (TEXT)
   - `claimed_by` (TEXT)
   - `claimed_at` (TIMESTAMP)

4. **`default_gear_templates`** - 預設裝備模板
   - `id` (UUID, PK)
   - `name` (TEXT)
   - `category` (TEXT)

5. **`trip_settings`** - 行程設定
   - `key` (TEXT, PK)
   - `value` (JSONB)

6. **`user_data`** - 用戶自訂資料
   - `id` (UUID, PK)
   - `user_id` (UUID, FK)
   - `key` (TEXT)
   - `value` (JSONB)
   - `locked` (BOOLEAN)
   - `can_edit` (BOOLEAN)

7. **`user_roles`** - 用戶角色（管理員權限）
   - `user_id` (UUID, PK)
   - `is_admin` (BOOLEAN)

8. **`shared_itinerary`** - 共享行程
   - `id` (UUID, PK)
   - `time` (TEXT)
   - `title` (TEXT)
   - `description` (TEXT)
   - `order` (INTEGER)

### Row Level Security (RLS)

所有資料表都啟用了 RLS，確保：
- 用戶只能查看/編輯自己的資料
- 管理員可以管理所有資料
- 共享資料（如行程、共享裝備）所有用戶都可查看

---

## 🎨 UI/UX 設計特色

### 手繪風格
- **字體：** Kiwi Maru, Zen Maru Gothic, Hachi Maru Pop
- **邊框：** 不規則手繪邊框（`.sketch-box`）
- **按鈕：** 和紙膠帶風格（`.washi-tape-button`）
- **背景：** 紙張紋理
- **動畫：** 3D 翻轉卡片（安全御守）

### 響應式設計
- 行動裝置優先設計
- 底部導航欄（行動裝置）
- 自適應布局

---

## 🚀 部署與設定

### 環境變數

在 `.env.local` 中設定：
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### Supabase 設定

1. **資料庫設定**
   - 執行 `supabase/schema.sql` 建立主要資料表
   - 執行相關的 migration 腳本

2. **Storage 設定**
   - 建立 `trip-images` bucket
   - 設定公開讀取權限

3. **Email 設定**
   - 修改 Email 模板，確認連結指向 `/auth/callback`
   - 設定 Site URL 和 Redirect URLs

### Vercel 部署

1. 連接 GitHub 倉庫
2. 設定環境變數
3. 自動部署

**相關文件：**
- `VERCEL_DEPLOYMENT.md` - 部署指南
- `VERCEL_REDEPLOY.md` - 重新部署指南
- `SUPABASE_SETUP.md` - Supabase 設定

---

## 📚 重要文件索引

### 設定指南
- `SUPABASE_SETUP.md` - Supabase 完整設定
- `ENV_SETUP.md` - 環境變數設定
- `ADMIN_SETUP.md` - 管理員設定
- `EMAIL_CONFIRMATION_SETUP.md` - Email 確認設定

### 功能說明
- `INVITE_FEATURE_GUIDE.md` - 邀請功能使用
- `USER_PERMISSION_MANAGEMENT.md` - 權限管理
- `DELETE_ACCOUNT_FEATURE.md` - 帳號刪除功能
- `HOW_TO_GRANT_ADMIN_PERMISSION.md` - 授予管理員權限

### 問題排除
- `TROUBLESHOOTING.md` - 常見問題
- `FIX_REGISTRATION_ERROR.md` - 註冊錯誤修復
- `FIX_EMAIL_REDIRECT_URL.md` - Email 重定向修復
- `FIX_EMAIL_TEMPLATE_REDIRECT.md` - Email 模板修復

---

## 🔐 權限系統

### 用戶角色
- **一般用戶：** 可查看和編輯自己的資料
- **管理員：** 可管理所有功能，包括：
  - 編輯共享行程
  - 管理預設裝備模板
  - 設定行程設定
  - 授予/撤銷管理員權限
  - 刪除用戶帳號
  - 解鎖用戶已鎖定的資訊

### 資料鎖定機制
- 用戶可以「鎖定」自己的緊急資訊和角色選擇
- 鎖定後，只有管理員可以解鎖或授予編輯權限
- 管理員可以隨時授予用戶編輯已鎖定資料的權限

---

## 🔄 即時同步

使用 Supabase Realtime 實現即時同步：
- 共享裝備認領狀態
- 共享行程變更
- 成員資訊更新
- 參與者列表更新

---

## 🎯 未來擴展建議

1. **通知系統** - 推播通知新成員加入、行程變更等
2. **聊天功能** - 團隊即時通訊
3. **天氣資訊** - 整合天氣 API
4. **地圖整合** - 顯示路線地圖
5. **照片分享** - 活動照片上傳與分享
6. **費用分攤** - 團隊費用計算與分攤

---

## 📞 技術支援

如遇到問題，請參考：
- `TROUBLESHOOTING.md` - 常見問題排除
- 各功能的詳細說明文件
- Supabase Dashboard 的日誌和錯誤訊息

---

## 📝 更新日誌

### 最新功能
- ✅ Email 確認成功通知
- ✅ 備用通知機制（時間戳檢查）
- ✅ 支援舊格式 Email 確認連結
- ✅ 參與者列表顯示
- ✅ 用戶權限管理
- ✅ 帳號刪除功能

---

**最後更新：** 2025-01-XX
**版本：** 1.0.0

